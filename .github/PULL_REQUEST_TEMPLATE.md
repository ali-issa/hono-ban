## Summary

<!-- What changes and why. Link the issue: Closes #N -->

## Checklist

- [ ] Tests added or updated (`*.test.ts`, and `*.test-d.ts` if types changed)
- [ ] `pnpm check` and `pnpm test` pass locally
- [ ] Changeset added (`pnpm changeset`) for any user-visible change
- [ ] Non-obvious decisions cite their source with `@ref`
- [ ] ADR added or updated if this changes public API, wire format, or runtime support
