# Changesets

Each file in this directory describes one user-visible change and the semver bump it needs. Create
one with `pnpm changeset`. They are consumed when a version PR is merged; see `CONTRIBUTING.md`.
